const React = require('react');

function Comment({}) {
  return (
    <ul>
      {/* {teas.map((comments) => (
        <li key={comments.id}>
          {comments.map((comment) => (
            <p key={comment.id}>
              {comment.text}
            </p>
          ))}
        </li>
      ))} */}
    </ul>
  );
}

module.exports = Comment;
